NEXT_PUBLIC_LIVEKIT_URL=wss://kero.ooo

# Optional: force TURN-only (useful for testing restrictive networks)
# You can also add `?relay=1` to any room URL to enable this at runtime.
NEXT_PUBLIC_LIVEKIT_FORCE_RELAY=0
